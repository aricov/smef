{{#fields}}
{{#eq quantifier.name "MANY"}}
{{builder}} {{name}}({{mapType typeSpec}}... values);
{{builder}} add{{capitalizeFirst name}}({{mapType typeSpec}} value);
{{^}}
{{builder}} {{name}}({{mapType typeSpec}} value);
{{/eq}}		
{{/fields}}
